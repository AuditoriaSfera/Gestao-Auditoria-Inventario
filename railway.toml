[build]
builder = "nixpacks"
buildCommand = "npm ci && npm run db:generate && npm run build"

[deploy]
startCommand = "npm run db:migrate:prod && npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

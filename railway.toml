[build]
builder = "nixpacks"
buildCommand = "npm run db:generate && npm run build"

[deploy]
startCommand = "npx prisma db push --schema prisma/schema/schema.prisma && npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
